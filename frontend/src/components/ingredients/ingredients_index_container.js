import { connect } from 'react-redux';
import IngredientsIndex from './ingredients_index';

const mSTP = state => ({

});


const mDTP = dispatch => ({

})

export default connect(
  mSTP,
  mDTP
)(IngredientsIndex);